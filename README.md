# is-works

A simple test npm package to check if everything works fine.  
Returns `true` only if the given string is `"works"`.

---

## 📦 Installation

You can install the package using **npm**:

```bash
npm install is-works
```

or using **yarn**:

```bash
yarn add is-works
```

---

## 🚀 Usage

```js
// Import the function
const isWorks = require("is-works");

// Test it
console.log(isWorks("works")); // true
console.log(isWorks("not works")); // false
```

---

## 🧠 Function Description

### `isWorks(string)`

**Parameters:**
- `string` *(string)* — Input value to test.

**Returns:**
- `boolean` — Returns `true` if the string is exactly `"works"`, otherwise `false`.

---

## 🌐 Links

- **NPM:** [npmjs.com/package/@devrohitsaini/is-works](https://www.npmjs.com/package/is-works)


